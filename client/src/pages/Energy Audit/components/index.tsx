// Mock components for Energy Audit Router
// These would be replaced with actual component implementations

import React, { ReactElement } from 'react';
import { Box, Typography } from '@mui/material';

// Placeholder components for the router
export const EnergyConsumptionAnalytics = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Energy Consumption Analytics</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const AnomalyDetection = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Anomaly Detection</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const AIRecommendations = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>AI Recommendations</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const PredictiveMaintenanceModel = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Predictive Maintenance Model</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const BuildingViewer = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Building Visualization</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const StandardsCompliance = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Standards Compliance</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const MobileDataCollection = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Mobile Field Data Collection</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const AuditWorkflowComponent = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Audit Workflow</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const ROICalculatorComponent = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>ROI Calculator</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const BenchmarkingComponent = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Benchmarking</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const ReportGeneratorComponent = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Report Generator</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
);

export const IntegrationHub = (): ReactElement => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h4" gutterBottom>Integration Hub</Typography>
    <Typography variant="body1">This component will be implemented as part of the Energy Audit system.</Typography>
  </Box>
); 